import pdb
from models.album import Album
from models.artist import Artist
import repositories.album_repository as album_repository
import repositories.artist_repository as artist_repository

# task_repository.delete_all()
# user_repository.delete_all()
# # tasks = task_repository.select_all()

# user1 = User("Malcolm", "Holwill")
# user_repository.save(user1)
# user2 = User("Chris", "Dryden")
# user_repository.save(user2)
# users = user_repository.select_all()
# found_user = user_repository.select(user2.id)
# # user_repository.delete(user1.id)

# user2.first_name = "Harrison"
# user_repository.update(user2)
# task1 = Task("Make coffee", user1.id, 5, False, None)
# task_repository.save(task1)
# task2 = Task("coffee", user1.id, 15, False, None)
# task_repository.save(task2)
# task3 = Task("Make pancakes", user1.id, 5, False, None)
# task_repository.save(task3)
pdb.set_trace()